<img class="img-fluid" src="{{ cover }}" alt="{{ title }}"/>
<div class="container"><div class="col-12 p-3 p-md-5">
<div class="p-3">
<h1><a href="{{ url }}">{{ title }}</a></h1>
{{ include('pos/cinema.html.twig') }}
<h3>{{ description }}</h3>
{{ include('pos/gallery.html.twig') }}
<p>{{ content }}</p>
<div class="text-center p-1">
<a href="{{ download }}" class="btn btn-light rounded">{{ cdownload }}</a>
</div></div></div></div>
{{ include('pos/video.html.twig') }}