<img class="img-fluid" src="{{ cover }}" alt="{{ title }}"/>
<div class="container">
<div class="col-12 p-3 p-md-5">
<div class="p-3">
<h1><a href="{{ url }}">{{ title }}</a></h1>
<div class="ratio ratio-16x9">
  <iframe src="https://www.youtube.com/embed/{{ video }}" title="{{ title }}" allowfullscreen></iframe>
</div>
<h3>{{ description }}</h3>
<p>{{ content }}</p>
<div class="text-center p-1">
<a href="{{ download }}" class="btn btn-light rounded">{{ cdownload }}</a>
</div>
</div>
</div>
</div>